version: '3.8'

services:
  llm_qwen:
    container_name: llm-dialog
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    ports:
      - "8080:8000"
    environment:
      - PYTHONPATH=/app
